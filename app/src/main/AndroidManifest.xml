<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parkIt">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_park_sm"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/logo_park_sm"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParkIt">
        <activity android:name=".UserSettingActivity"></activity>
        <activity android:name=".ResetActivity" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/Theme.ParkIt.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.ParkIt.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.ParkIt.NoActionBar"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>